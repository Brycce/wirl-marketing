// The only new lines on this page. Everything else comes from the site's
// approved copy (src/components/copy.ts). These three are connect_after,
// split into steps; cut them if the page runs long.
export const steps = [
  'Install once.',
  'Approve it in the browser, the first time.',
  'Ask it to deploy.',
];
